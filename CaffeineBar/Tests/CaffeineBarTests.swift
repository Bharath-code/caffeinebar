import Testing
@testable import CaffeineBar

@Suite("CaffeineBar Tests")
struct CaffeineBarTests {
    @Test("App target compiles successfully")
    func appCompiles() {
        // Placeholder — verifies the test target links against the main target.
        #expect(true)
    }
}
